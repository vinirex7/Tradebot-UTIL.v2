# Tradebot-UTIL.v2 — Dependências extras para backtest e análise
# Instalar com: pip install -r requirements.txt -r requirements_backtest.txt

# Inclui todas as dependências do requirements.txt
-r requirements.txt

# ── Visualização ────────────────────────────────────────────
matplotlib>=3.8.0

# ── Análise avançada (opcional) ─────────────────────────────
scipy>=1.12.0
